<%= form_for(@ms_offer) do |f| %>
  <% if @ms_offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ms_offer.errors.count, "error") %> prohibited this ms_offer from being saved:</h2>

      <ul>
      <% @ms_offer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :total %><br />
    <%= f.text_field :total %>
  </div>
  <div class="field">
    <%= f.label :batch_id %><br />
    <%= f.text_field :batch_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
