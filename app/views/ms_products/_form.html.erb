<%= form_for(@ms_product) do |f| %>
  <% if @ms_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ms_product.errors.count, "error") %> prohibited this ms_product from being saved:</h2>

      <ul>
      <% @ms_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :total %><br />
    <%= f.number_field :total %>
  </div>
  <div class="field">
    <%= f.label :batch_id %><br />
    <%= f.text_field :batch_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
